terraform {
  backend "s3" {
    bucket       = "replace-with-terraform-state-bucket"
    key          = "repository-mcp/REPLACE_WITH_ENVIRONMENT/opentofu.tfstate"
    region       = "us-east-1"
    encrypt      = true
    use_lockfile = true
  }
}
