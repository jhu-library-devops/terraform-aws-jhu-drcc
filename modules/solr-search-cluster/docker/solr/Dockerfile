# Solr image built from upstream
# Override SOLR_VERSION at build time to pin a specific release
ARG SOLR_VERSION=9.7

FROM solr:${SOLR_VERSION}

# Copy any custom configsets or scripts here
# COPY configsets/ /opt/solr/server/solr/configsets/
