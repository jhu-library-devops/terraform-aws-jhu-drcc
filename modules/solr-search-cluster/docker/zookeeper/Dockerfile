# Zookeeper image built from upstream
# Override ZK_VERSION at build time to pin a specific release
ARG ZK_VERSION=3.9

FROM zookeeper:${ZK_VERSION}

# Add custom configuration if needed
# COPY zoo.cfg /conf/zoo.cfg
