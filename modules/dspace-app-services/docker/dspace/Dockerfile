# DSpace backend image built from upstream
# Override DSPACE_VERSION at build time to pin a specific release
ARG DSPACE_VERSION=8

FROM dspace/dspace:${DSPACE_VERSION}

# Add site-specific customizations here
